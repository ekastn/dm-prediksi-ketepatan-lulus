# Optional / Unused Data

File `dataset.csv` adalah salinan dari `3-data-preparation/dataset.csv` (608 rows × 27 kolom, 
raw sebelum preprocessing). 

**Status:** Tidak digunakan dalam experiment ini. Disimpan untuk referensi / debugging.
Preprocessing menggunakan file ini sebagai input menghasilkan `dataset_clean.csv`.

Data source yang sesungguhnya tetap di `3-data-preparation/`.
